# packages/

CLI/SDK를 위해 예약된 디렉터리. 이름과 구성은 아직 미정이다(의도적 미정 상태 —
`docs/ROADMAP.md`의 `MONO-06` 참고). `pnpm-workspace.yaml`의 `packages/*` glob이
이미 이 디렉터리를 workspace 대상으로 잡고 있으므로, 실제 패키지를 추가할 때
별도 workspace 설정 변경은 필요 없다.
